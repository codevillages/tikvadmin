# 恢复为 Nginx 默认的 nobody 用户（root 运行容器时无需指定 appuser）
user nobody;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # 前端静态资源配置
    server {
   # 显式指定监听所有IP的3002端口（等价于 listen 3002;，但更清晰）
        listen 0.0.0.0:3002 default_server;
        # 匹配所有Host头（localhost、192.168.1.100、容器IP等）
        server_name _;

        # 前端静态文件根目录
        root /usr/share/nginx/html;
        index index.html;

        # 处理前端路由刷新问题
        location / {
            try_files $uri $uri/ /index.html;
        }

        # 反向代理后端 API 请求
        location /api/ {
            # Preserve the /api prefix when proxying to the backend.
            proxy_pass http://127.0.0.1:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
